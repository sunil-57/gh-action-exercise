name: Deployement Exercise 1
on: push
jobs:
    lint:
        runs-on: ubuntu-latest
        steps:
            - name: Get Code
              uses: actions/checkout@v3
            - name: Install dependencies
              uses: npm ci
            - name: Lint
              uses: npm run lint
    test:
        runs-on: ubuntu-latest
        steps:
            - name: Get Code
              uses: actions/checkout@v3
            - name: Install dependencies
              uses: npm ci
            - name: Test code
              uses: npm run test
    deploy: 
        runs-on: ubuntu-latest
        steps:
            - name: Get Code
              uses: actions/checkout@v3
            - name: Install dependencies
              uses: npm ci
            - name: Build Project
              uses: npm run build
            - name: Deploy code
              uses: echo "Deploying ...."
